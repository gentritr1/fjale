{
  "name": "fjale",
  "version": "1.0.0",
  "private": true,
  "license": "GPL-2.0-or-later",
  "type": "module",
  "description": "FJALË — loja shqipe e fjalës së ditës.",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check service-worker.js && node --check src/app.js && node --check src/config.js && node --check src/game.js && node --check src/words.js && node --check src/accepted-words.js && node scripts/validate-lexicon.mjs && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
